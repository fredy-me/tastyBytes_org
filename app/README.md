# `app/`

Application code organized by responsibility.

Intended contents:
- `Controllers/` request/response handlers
- `Models/` database access and domain objects (`User`, `Recipe`, `Favorite`)
- `Views/` UI templates

